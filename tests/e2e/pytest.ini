[pytest]
markers =
    slow: tests that exercise the BGE-M3 ONNX session and run cold delta-syncs in-band; expect 30-120s per project. Use `pytest -m slow` to run only these, or `pytest -m "not slow"` to skip.
