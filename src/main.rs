//! `schema` — MCP server for indexing project specs, ADRs, and contracts.
//!
//! FASE 1.0 — bootstrap entry. Subsequent commits add MCP wiring, corpus indexing,
//! embeddings, retrieval, and tools.

fn main() {
    println!("schema 0.1.0 — bootstrap (commit 1/10)");
}
